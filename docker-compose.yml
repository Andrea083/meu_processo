version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: tg_legaldb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: J0403366#
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: